export interface Environment {
  production: boolean;
  firebase: {
    apiKey: string;
    databaseURL: string;
    // authDomain: string;
    // projectId: string;
    // storageBucket: string;
    // messagingSenderId: string;
    // appId: string;
    // measurementId: string;
  };
}
